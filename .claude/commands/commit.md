Commit current changes:

1. Run `git status` to review changes
2. Run `git diff --stat` for summary
3. Generate semantic commit message
4. Stage and commit: `git add -A && git commit -m "message"`
5. Update status.md with commit note
