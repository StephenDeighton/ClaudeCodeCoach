{
  "permissions": {
    "allow": [
      "Bash",
      "Read",
      "Write",
      "Edit",
      "Glob",
      "Grep",
      "TodoRead",
      "TodoWrite",
      "Task",
      "Skill"
    ],
    "deny": [
      "Bash(git push origin main*)",
      "Bash(git push origin master*)",
      "Bash(git push --force*)",
      "Bash(rm -rf /)*",
      "Bash(rm -rf .git*)",
      "Bash(rm -rf ~*)"
    ]
  }
}
